{
  "name": "@moregpu/client",
  "version": "0.1.0",
  "description": "Client SDK for the MoreGPU pool — submit tensor/compute jobs, batch them, and read fleet contribution. Works in Deno, Node, and browsers.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc -b", "test": "vitest run" },
  "license": "Apache-2.0"
}
